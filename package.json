{
	"name": "turbo-demo",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"build": "turbo run build",
		"docker": "turbo run docker",
		"dev": "turbo run dev",
		"dev:local": "turbo run local-db:start && turbo run dev:local"
	},
	"workspaces": [
		"apps/*",
		"databases/*",
		"packages/*"
	],
	"devDependencies": {
		"@auto-it/docker": "^10.37.1",
		"auto": "^10.37.1",
		"rimraf": "^3.0.2",
		"turbo": "^1.2.16"
	}
}
